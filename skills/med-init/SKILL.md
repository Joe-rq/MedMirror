---
name: med-init
description: 初始化或恢复 MedMirror 工作环境
---

# 初始化或恢复 MedMirror 工作环境

读取 AGENTS.md 与 .42cog/、状态板。检查目录和标准指针，仅补缺失文件，不覆盖原件。确认密钥文件被忽略但不读取值。运行清单检查，报告缺项与下一步；不启动模型调用。

完成后更新 state/board.md；临床合理性无法判断时提交复核，不自行裁决。
