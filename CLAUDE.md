# MedMirror 工作规约

## 开工与授权
先读 .42cog/ 四份、specs/calibration.md、state/board.md、state/memory/MEMORY.md。方向只引用 .42cog/intent.md。
每轮简述行动，完成后报告结果。已确定范围内的开发、离线测试、修复和文档更新连续执行，不逐小任务等待确认。变更研究问题、放宽验收、超预算、对外发布或破坏已有成果时交给用户决定。遵守运行环境权限，不能用换工具绕过限制。不要自行提交、推送或创建远程仓库。

## 方法与标准
先走 skills/med-research，再走 med-proto；初始化与演化分别见 med-init、med-evolve。规则检查交给脚本，含义判断附原文，医学判断保留专业复核。先 CLI 后展示；不要为了模板安装无关工具。多模型评审的调用也计费，密钥与价格未配置不启动付费评审。换谱系评审未做时如实记账，不把自检冒充独立评审。

## 目录与真相源
.42cog/ 管项目事实与方向；specs/ 管作品标准；src/ 管程序；runs/ 管原始试次、派生结果和报告；state/ 管进度；vault/raw/ 保存不可修改的历史原件；vault/truth/ 保存带出处事实；notes/、resources/ 按来源授权使用；scripts/ 和 skills/ 管方法；docs/ 管长期记录；_tmp/、_build/、_archive/ 管可丢弃过程材料。
不要把不可重建的实验原始回答放临时区。runs/ 自 2026-09-13 起入 Git（issue #4 主人拍板：远程仓库即备份落点，伙伴 clone 取回；密钥类文件仍被 .env 规则排除在外）；scripts/backup_data.py 提供本地双保险与恢复演练。

## 评测护栏
协议和评分版本固定后再跑基线；探索追问单独汇总。未提及与反对分开，失败不按未提及计数。保留全部计划试次状态、分母、原文定位和反例。评分 JSON 合法不代表评分正确。证据被提到不等于已核实，不输出未经专业复核的已确认 Bias。

## 调用与预算
只使用 .42cog/real.md 指定模型和官方 API，不静默替换。正式调用前确认费率、币种、参数和最大输出，预留费用后调用，完成后结算。有限重试与追问共同受总额限制；价格未知则离线推进。保持原始回答与配置，恢复不重复成功试次，重放不请求被测模型。
密钥只放 .env.local 或环境变量；不读出、打印或写入报告，不进版本库。真实患者信息不用于本轮合成实验。

## 状态与变更
完成可命名工作单元后更新 state/board.md；重要决定写 state/changelog.md。人写的 hi.md、notes 原件只读。已确认意向的实质变化由用户决定。新增能力须通过相关反例和回归，不能通过降低标准让旧实现过关。

