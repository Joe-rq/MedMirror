# 决策点 · 在50元预算内如何确定首轮MedMirror实验协议、官方API接入与评分方案

> 2026-09-09。**这一份是给 AI 用的裁刀，不是给你答的题。**
> 先写下不可妥协的几条，让候选逐格回答；**允许大量功能根本不读**。
> ⚠️ 三条草稿由 AI 先填，**你只改这三条**。改完再往下走。

## 我要作的决定

在50元预算内如何确定首轮MedMirror实验协议、官方API接入与评分方案

## 一、这件事上，我不可妥协的是哪几条

逐条编号。每条要能被「原生支持 / 部分 / 没有」回答，答不了的说明写得太虚。

| # | 不可妥协的 | 为什么它是命门 | 怎么算满足 |
|---|---|---|---|
| 1 | 首轮实验必须可比、可复现、可回溯 | 若提示与参数不可比，差异无法解释；若无原文定位，发现无法复核 | 固定病例、提示变体、消息结构、实际模型标识、参数、日期、重复次数；每个发现可回到 trial 原文 |
| 2 | 三家官方 API 必须先完成最小接入与成本核对 | 价格、usage 或参数未知就无法守住 50 元预算 | 三家各完成一次最小请求；记录 base URL、型号、鉴权、usage、错误、价格和币种；未知时不启动批量调用 |
| 3 | 评分必须区分观察与医学裁决 | 结构化 JSON 合法不代表语义正确；团队暂无专业医学复核 | 未提及／反对／条件支持／明确推荐分开；标签附原文；人工抽查小样本；报告不写“已确认 Bias” |

## 二、知道了它们，我要作的是哪一个决定

> 冻结一个合成颈动脉斑块场景、三种首轮提示和三家指定模型的最小协议，先用离线样本与最小 API 请求验证链路，再在预算内执行基线和有界追问。

## 三、哪些看起来相关，但知道了也不改变这个决定

**排除项。一条不看的，抵十条要看的。**

- 复杂多 Agent、向量数据库、前端选型、更多病例和模型排行榜；它们不改变首轮是否能产出可复核发现包。

## 四、我的预判

**不必假装客观。** 写下你现在的猜测，让 AI 回源码去推翻它。

| 我怀疑 | 请核实 |
|---|---|
| 三次重复是否足够支持“稳定差异” | 回到实验设计与原始 trial，首轮只报告描述性频率，不宣称统计稳定 |
| 三家 API 是否都能返回足够的 usage 与价格信息 | 查官方文档，并各做一次最小请求；未知项不得用猜测填补 |

> 通行建议是隐藏立场以免诱导模型。这里反过来——**亮出立场，但把任务改造成可证伪的核验。**

---

**放行**：三条命门都填了、都能被「有 / 部分 / 没有」回答，就可以开始取材。
