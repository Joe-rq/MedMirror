# 实际账单核对记录（issue #3 关闭条件之一）

> 状态：**待主人填写**。三家控制台核对实际消费/额度变动后填入；差额超出估算的部分单独列出原因。填完后本文件与 `scripts/reconcile_budget.py` 输出的工程核对报告共同构成 #3 的关闭依据。

## 核对口径

- 时间范围：2026-09-09 至 2026-09-12（基线采集 + 开发期间的最小/诊断调用）
- 估算基线：`uv run python scripts/reconcile_budget.py --trials docs/experiments/exp003-baseline/result/trials.jsonl`
- 保守假设：DeepSeek 按高峰价（实际可能在闲时段消耗）；StepFun/GLM 按登记当前价

## 三家实际消费

| 供应商 | 控制台显示消费/额度变动（元） | 与估算差额（元） | 差额原因 |
|---|---:|---:|---|
| DeepSeek | 待填 | 待算 | （如：闲时价格、历史重试次数、exp002 诊断调用） |
| StepFun | 待填 | 待算 | |
| GLM | 待填 | 待算 | |

## 历史调用差额

- exp002 连通性测试：3 次最小请求（8 token 上限），金额极小但未逐笔记入
- exp001 冒烟：离线 fixture，无真实调用
- 开发期间失败/重试：次数与金额待查控制台调用日志

## 结论

- [ ] 三家实际总消费 ≤ 估算上界（0.3720 元）+ 合理差额
- [ ] 差额原因已逐项列出（不填零）
- [ ] 50 元预算余额确认
- [ ] 程序硬闸工程验收通过（见 `tests/test_runner.py` 预算场景 + `tests/test_budget_gate.py`）
